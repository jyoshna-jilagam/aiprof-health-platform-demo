# Flask session secret (generate a random string for production)
SECRET_KEY=change-this-to-a-random-secret

# Optional: enable LLM-based intent extraction for the AI agent.
# If unset, the app runs fully with the deterministic fallback (clearly labeled in the UI).
ANTHROPIC_API_KEY=
